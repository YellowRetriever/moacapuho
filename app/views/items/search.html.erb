 <body>
    <div class="container">
      <div class="alert alert-warning" role="alert"><h1>検索結果</h1></div>
        <% @results.each do |item| %>
        
          <div class="media pt-3">
            <div class="media-body">
              <h5 class="text-info"><%= link_to item.name, item_path(item) %></h5>
              <p><%= item.text %></p>
            </div>
          </div>
        <% end %>
        <%= link_to "戻る", top_path %>
    </div>
  </body>
